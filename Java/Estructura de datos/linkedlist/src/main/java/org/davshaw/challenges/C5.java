package org.davshaw.challenges;

import org.davshaw.classes.doublelinkedlist.Node;
import org.davshaw.classes.megalinkedlist.MegaLinkedList;

public class C5
{
    //Función que reciba una lista enlazada y la rote n posiciones
    public void result(MegaLinkedList lista, int rotaciones)
    {
        //El enfoque a utilizar: x1 rotación se ejecuta lo siguiente
        //El prev de mi head (Tail) será mi nuevo head, y el tail anterior será el prev de mi nueva head
        for (int i = 0; i < rotaciones; i++)
        {
            /*
            ! El proceso será el siguiente
            ? 1) Obtener el Tail
            ? 2) 
            */
        }
    }    
}
